# Configuration file for Kong that will be dropped in place with Kongfig

apis:
  - name: mockbin
    attributes:
      upstream_url: "http://mockbin.com"
      request_host: "mockbin.com"

  - name: buttons
    attributes:
      upstream_url: "http://payments:3000"
      uris: [ "/buttons" ]
      strip_uri: true
